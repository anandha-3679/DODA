[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "doda"
version = "0.1.0"
description = "Decoupled Operator Driven Architecture"
requires-python = ">=3.9"

dependencies = [
    "numpy",
    "pandas",
    "scikit-learn"
]